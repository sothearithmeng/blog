/**
 * {@code @Service} classes that implement interfaces in the service package.
 * Other layers should use the interface, so an implementation can change later.
 */
package com.kshrd.blog.service.impl;
