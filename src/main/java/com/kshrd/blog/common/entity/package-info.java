/**
 * Base JPA types shared across entities in every layer/module, such as common id and
 * audit-timestamp columns. Not tied to any single feature's {@code entity} package.
 */
package com.kshrd.blog.common.entity;
