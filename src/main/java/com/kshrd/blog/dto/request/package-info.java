/**
 * Objects that describe data received by an API endpoint. Add validation
 * annotations such as {@code @NotBlank} here so invalid input is rejected early.
 */
package com.kshrd.blog.dto.request;
